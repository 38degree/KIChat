# TTS Service Dependencies
# Note: setuptools + wheel are pre-installed in Dockerfile
fastapi==0.115.*
uvicorn[standard]==0.34.*
python-multipart==0.0.*

# PyTorch (pre-built wheels to avoid compilation)
torch>=2.5
torchaudio>=2.5

# Coqui TTS installed separately in Dockerfile (--no-build-isolation)
# Audio
soundfile==0.13.*
numpy>=1.26
scipy>=1.11

# Utilities
loguru==0.7.*
aiofiles==24.*
tqdm>=4.66
